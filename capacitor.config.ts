import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'belajar-mobile-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
